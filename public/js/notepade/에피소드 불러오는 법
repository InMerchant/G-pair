스크립트 모듈형식으로 작성 후 사용

import { getEpisodeData } from './js/upload,loading/episodeSearch.js';
//에피소드 불러오는 함수(webtoonID, episodeID 값 넣어주기)
getEpisodeData(webtoonID, 2).then(episodeData => {
    if (episodeData) {

    }
});